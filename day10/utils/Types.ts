export type PipeArray = Array<Array<string>>;
export type CoOrdinate = Array<number>;

export type PipeConnectionDetails = {
    distanceArray: Array<Array<Number>>
    foundConnections: Array<CoOrdinate>
}